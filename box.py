# Attributes
#   Coordinates for each line (Top, Left, Bottom, Right)
#       self.coordinates = [(x,y), (x+1,y), (x,y+1), (x+1,y+1)]
#   Tracks if a box's line is filled
#   Tracks which player completed this box
#   Tracks if it's completed

class Box:
    # Initializer (x,y for top left corner)
    def __init__(self, x, y):
        # TODO: Gisele
        return

    # Check if given coordinates form a line of the current box
    def connect(self, coordinates):
        # TODO: Gisele
        return
